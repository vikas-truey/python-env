name: Deployment-prod

on:
  push:
    branches:
      - main

jobs:
  deployment:
    runs-on: ubuntu-latest
    environment: Prod
    steps:
      - name: deploy
        run: |
          echo "hellow"
